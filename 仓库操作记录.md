# 仓库操作记录

本仓库（ExpenseTracker / 记账）的操作手册与历史记录。涉及密钥、密码的内容只记录位置，永不记录明文。

## 基本信息

| 项目 | 值 |
|---|---|
| 远程仓库 | https://github.com/Ibis919/ExpenseTracker |
| 本地路径 | `D:\ExpenseTracker`（必须纯英文路径，Android 构建拒绝中文路径） |
| 分支 | `main` |
| 更新源 | jsDelivr CDN（主）+ GitHub Releases（兜底） |

## 构建环境（本机）

| 组件 | 位置 |
|---|---|
| JDK 21 | `C:\Users\ibis\.jdks\jbr-21.0.11`（Android Studio 自带 JBR，注意 Studio 主 JVM 是 25，构建须用 21） |
| Android SDK | `D:\Android\Sdk` |
| Gradle 缓存 | `D:\Android\gradle-home`（腾讯镜像源，免代理直连） |
| 签名密钥 | `D:\ExpenseTracker\keystore\release.keystore` + `keystore.properties`（已 gitignore，**丢失即无法发更新**，需异地备份） |

命令行构建 release：

```powershell
$env:JAVA_HOME='C:\Users\ibis\.jdks\jbr-21.0.11'
$env:GRADLE_USER_HOME='D:\Android\gradle-home'
.\gradlew.bat assembleRelease --no-daemon
```

2026-09-12 起已把 `org.gradle.java.home=C:/Users/ibis/.jdks/jbr-21.0.11` 写入用户级 `C:\Users\ibis\.gradle\gradle.properties`，任何终端（含 TRAE）跑 gradlew 都自动用对 JDK，JAVA_HOME 不再必须手动设置（坑：TRAE 终端 PATH 里的 java 是精简 JRE 缺 jlink，Gradle 新起守护进程用它会导致 compileReleaseJavaWithJavac 失败）

产物：`app\build\outputs\apk\release\app-release.apk`

## 发版流程（v0.7.1 起）

1. `app/build.gradle.kts` 改 `versionCode` + `versionName`
2. 命令行构建 release（见上）
3. **拷贝 APK 进仓库 CDN 位**：`Copy-Item app\build\outputs\apk\release\app-release.apk apk\latest.apk -Force`（**不能漏！** v0.9.1 事故就是漏了这步：CDN tag 直链一直服务旧包，用户检查到 0.9.1 下载到的却是 0.9.0）
4. 更新根目录 `update.json` 的 `version` / `notes` / `apkUrl`（apkUrl 用 tag 直链 + gcore 域名，每版要改，如 `https://gcore.jsdelivr.net/gh/Ibis919/ExpenseTracker@vX.Y.Z/apk/latest.apk`）
5. README 版本历史表加一行
6. `git add -A && git commit -m "..." && git push`（**需 FlClash 开规则模式**，git 代理指向 127.0.0.1:7890）
7. GitHub 发 Release：`gh release create vX.Y.Z "app\build\outputs\apk\release\app-release.apk#记账 vX.Y.Z (正式签名)" --title "vX.Y.Z" --notes "..."`
8. **刷新 jsDelivr 缓存**（关键，否则用户最长 24 小时检测不到新版）：
   ```
   https://purge.jsdelivr.net/gh/Ibis919/ExpenseTracker@main/update.json
   https://purge.jsdelivr.net/gh/Ibis919/ExpenseTracker@main/apk/latest.apk
   https://purge.jsdelivr.net/gh/Ibis919/ExpenseTracker@vX.Y.Z/apk/latest.apk
   ```
   浏览器直接打开这三个 URL 即可
   **注意限流**：purge 对同一路径有冷却期（返回 `throttled:true` 时该文件被跳过不刷新）。必须确认返回里对应项 `throttled: false` 且 CF/FY 都刷新，否则等冷却后重试
9. 验证：update.json 版本号正确；**下载 `@vX.Y.Z/apk/latest.apk` 与本地构建 SHA256 比对**（v0.9.1 事故中两版包字节数恰好相同，只查大小会漏检）

## 签名史（重要）

- v0.5.x 及更早：release 也用 debug keystore 签名
- v0.6.0 起：正式 keystore（`keystore/release.keystore`，RSA 2048）
- **后果**：debug 签名旧版 → v0.6.0+ 无法覆盖安装，必须卸载重装 + CSV 迁移数据（一次性）
- v0.6.0 起所有版本同签名，正常覆盖升级

## 网络环境备忘

- **推送 GitHub**：FlClash 规则模式（全局会把国内镜像也代理掉，导致 Gradle 卡死）
- **git 全局代理**：`http.proxy / https.proxy = 127.0.0.1:7890`（FlClash 关闭时推送必失败）
- **Gradle 依赖**：腾讯镜像（`mirrors.cloud.tencent.com`），免代理直连，不要开全局代理
- **github.com 443 直连**：SNI 阻断，不可用；`api.github.com`（gh 命令）多数时候可直连

## 历史事件日志

| 日期 | 事件 |
|---|---|
| 2026-09-02 | 项目创建；因中文路径 `D:\记账` 触发 AGP 报错，整体迁移至 `D:\ExpenseTracker` |
| 2026-09-02 | v0.1.0–v0.3.2：MVP → 底部工具栏 → 左滑删除卡片 → 商业化美化/统计页/预算通知/自动备份 |
| 2026-09-02 | 列表滚动卡顿优化（去 IntrinsicSize 双测量 + release 构建） |
| 2026-09-02 | GitHub 仓库建立，代码 + README + release v0.3.2 上线 |
| 2026-09-02 | 配置正式签名 keystore；授权 MIT 开源 |
| 2026-09-09 | v0.4.0 全库搜索（日期/关键词/金额，空格组合） |
| 2026-09-09 | v0.5.x：微信接收文件「用其他应用打开」直达导入；导出双通道 |
| 2026-09-10 | v0.6.0 代付记账（excluded 字段，Room v1→v2 迁移） |
| 2026-09-11 | v0.7.0 应用内更新（GitHub API 单源） |
| 2026-09-12 | v0.7.1 更新源 CDN 化：jsDelivr 主源（免 VPN）+ GitHub 兜底，`apk/latest.apk` + `update.json` 入库 |
| 2026-09-12 | v0.7.2 修复检查更新按钮状态死锁（MutableState 误比较导致永远「检查中」）+ 版本号独立显示 |
| 2026-09-12 | 发现 jsDelivr `@main` 分支缓存滞后：purge 只清边缘（CF/FY），源站分支解析最长 12h 才刷新。**待办：下版加版本号语义比较守卫，CDN 返回旧版本号时不弹「降级」更新框** |
| 2026-09-12 | v0.8.0：回收站（软删除 deletedAt，30 天自动清除，DB v2→v3）+ 月度环比卡片 + 周期性支出表（recurring_expenses，每月自动记账幂等）；更新机制加固完成（双源取新 + 版本语义比较 + 下载地址改 tag 不可变直链），上表待办已消化。**注意：本版仅本地构建，未推送未发 Release（会话中断），随 v0.9.0 一并上线** |
| 2026-09-12 | v0.9.0：记账模板（templates 表，记一笔页点按带入/长按删除）+ 自定义分类（categories 表，增删改名 + emoji 选择，删除时记录归入「其他」，DB v3→v4，新装 onCreate 播种默认分类，CSV 导入自动补建分类）+ 统计页趋势图升级为折线图（6/12 月切换，当前月高亮）；本次发布包含未发布的 v0.8.0 内容，用户从 v0.7.x 直接跨到 v0.9.0 |
| 2026-09-12 | v0.9.0 发布后问题：update.json 的 purge 被 jsDelivr 限流跳过（上次 v0.7.1/0.7.2 发版刷过，冷却期内），国内边缘节点仍缓存 v0.7.1 时期的 update.json → 手机检查更新显示 0.7.1 或「已是最新」。APK 的 tag 直链 purge 成功不受影响。处理：等待冷却后重试 purge 直到 itemsPurged≥1。用户实测：换 Wi-Fi 后正常检测到 0.9.0，数据迁移无损 |
| 2026-09-12 | v0.9.1：检查更新提速——改为多源并行检测（jsDelivr 三个 CDN 入口 cdn/fastly/gcore + GitHub API 同时发起，谁快用谁、谁新用谁），CDN 超时 8s→4s、GitHub 5s→3s、总检查上限 6s 封顶；典型耗时从 6-13 秒降到 2-3 秒。同时解决单个 CDN 节点缓存滞后时检测不到新版的问题（三入口交叉兜底） |
| 2026-09-12 | v0.9.1 事故：发版漏了流程第 3 步（APK 拷贝进仓库 `apk/latest.apk`），CDN tag 直链仍服务 v0.9.0 包 → 用户检查到 0.9.1 但下载安装的是 0.9.0（装完 versionName 还是 0.9.0）。两包大小恰好相同（10,984,794 字节），仅查大小的验证被骗过。另：免 VPN 时 CDN 大文件下载进度条卡死（跨境链路 QoS），开 VPN 可下。处理：跳版发 v0.9.2 修正 |
| 2026-09-12 | v0.9.2：APK 下载多域名轮换（原 URL → cdn/fastly/gcore 依次尝试 + GitHub 兜底，readTimeout 30s→20s 加速卡死切换），apkUrl 改用 gcore.jsdelivr.net 域名（国内大文件下载更友好）；发版流程第 3 步加粗警示 + 验证改 SHA256 比对 |
